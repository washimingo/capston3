import { PorVencerPipe } from './por-vencer-pipe';

describe('PorVencerPipe', () => {
  it('create an instance', () => {
    const pipe = new PorVencerPipe();
    expect(pipe).toBeTruthy();
  });
});
